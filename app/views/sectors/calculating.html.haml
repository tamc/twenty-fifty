%script{ :type => 'text/javascript' }
  $(document).ready(function() {
  = redirect_to_disclaimer
  = scroll_to_last_position(false) # False means: Remember current position for reload once calcualted
  = poll check_if_calculated_pathway_sector_url(@code,@choice)
  })

#demand
  .chart#demand_chart
  .switch &nbsp;    

  = render 'pathways/demand_choices'
  
#supply
  .chart#supply_chart
    .loading Calculating
    #pathwayPollingCounter 0s    
  .switch &nbsp;

  = render 'pathways/supply_choices'

#ghg
  
  .chart#emissions_chart
  .switch &nbsp;
  
  = render 'pathways/ghg_choices'
  
  #keydata= render 'keydata'

  
.clear